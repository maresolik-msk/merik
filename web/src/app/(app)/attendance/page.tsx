import { AttendanceView } from "@/features/attendance/attendance-view";

export const metadata = { title: "Attendance — Merik" };

export default function AttendancePage() {
  return <AttendanceView />;
}
